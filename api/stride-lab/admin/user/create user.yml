info:
  name: create user
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/admin/dashboard/users"
  body:
    type: json
    data: |-
      {
        "name": "Torrada Afornali",
        "email": "torrara@toastlabs.com.br",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
