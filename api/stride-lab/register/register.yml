info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Matheus Afornali",
        "email": "matheus@toastlabs.com.br",
        "password": "password",
        "password_confirmation": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
