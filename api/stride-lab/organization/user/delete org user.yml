info:
  name: delete org user
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_URL}}/dashboard/users/{{userId}}"
  auth: inherit

runtime:
  variables:
    - name: userId
      value:
        type: number
        data: "12"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
