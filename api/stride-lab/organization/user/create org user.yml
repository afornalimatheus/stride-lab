info:
  name: create org user
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/dashboard/users"
  body:
    type: json
    data: |-
      {
        "name": "Torrada Afornali",
        "email": "torrada5@toastlabs.com.br",
        "password": "password",
        "role": "MANAGER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
