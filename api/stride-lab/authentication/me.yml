info:
  name: me
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/auth/me"
  body:
    type: json
    data: |-
      {
        "email": "matheus@toastlabs.com.br",
        "password": "Torrada1803!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
