info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "matheus@toastlabs.com.br",
        "password": "Torrada1803!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
